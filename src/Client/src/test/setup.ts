import "@testing-library/jest-dom";
import "@/lib/i18n";

// Mock window.matchMedia for jsdom (used by ThemeProvider and responsive hooks)
Object.defineProperty(window, "matchMedia", {
  writable: true,
  value: (query: string) => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: () => {},
    removeListener: () => {},
    addEventListener: () => {},
    removeEventListener: () => {},
    dispatchEvent: () => false,
  }),
});
